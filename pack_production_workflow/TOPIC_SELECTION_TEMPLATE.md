# Topic Selection Template

Fill this in before starting research on a new Official Knowledge
Pack. Its purpose is to catch a bad topic choice before any research
or writing time is spent on it. The full acceptance criteria this
worksheet applies are defined once, canonically, in
`QUALITY_STANDARD.md`'s "Topic Acceptance Criteria" section - this
template is the worksheet, not a second copy of the policy.

## Proposed pack

- **Working title:**
- **One-sentence theme:** *(what ties this pack's articles together -
  e.g. "signaling and orientation without infrastructure")*
- **Proposed `pack_id`:** *(must use the `hv_official_` prefix; check
  it isn't already used by a folder in `official_packs/`)*
- **Categories this pack will span:** *(one or more of the 11 valid
  category display names)*
- **Planned article list** *(working titles - a pack should have more
  than one article; a single-article "pack" is usually better as a
  core corpus article instead - see Criterion 1 below)*:
  1.
  2.
  3.

## Acceptance criteria checklist

Apply each criterion from `QUALITY_STANDARD.md`; record the answer
here, not just a checkmark - a future reader should be able to see
*why* this topic passed.

- [ ] **Criterion 1 - Multi-article coherence:** this pack has a real,
      articulable theme spanning multiple articles, not a single
      article dressed up as a pack.
  - Why it passes:
- [ ] **Criterion 2 - No duplication:** the topic doesn't already
      exist as a core corpus article (check `knowledge/<category>/`)
      or another official pack (check `official_packs/`).
  - Checked against:
- [ ] **Criterion 3 - Plausible source availability:** at least one
      Tier 1 or Tier 2 source (per `QUALITY_STANDARD.md`'s source
      quality hierarchy) plausibly exists for the pack's central
      claims, before research even starts.
  - Candidate sources believed to exist:
- [ ] **Criterion 4 - Category fit:** every planned article fits one
      of the 11 existing category display names; no new category is
      being invented.
- [ ] **Criterion 5 - Scope safety:** the topic does not require
      tactical, weapon-making, trap-making, or evasion content to be
      useful (see `QUALITY_STANDARD.md`'s safety content rules).
- [ ] **Criterion 6 - Roadmap alignment (if applicable):** if this
      topic overlaps with an item in
      `docs/Knowledge_Expansion_Roadmap_v1.md`, note it here - a pack
      is a reasonable way to cover a roadmap topic in more depth than
      a single core article would, but should not silently duplicate
      a roadmap item already planned as a core article.

## Decision

- [ ] **Accepted** - proceed to `RESEARCH_PLAN_TEMPLATE.md`.
- [ ] **Rejected / deferred** - reason:

- **Decided by:**
- **Date:**
